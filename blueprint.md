
# 專案藍圖：數獨遊戲

## 專案概覽

這是一個使用現代 Web 技術（HTML、CSS、JavaScript）建立的互動式數獨遊戲。專案目標是提供一個美觀、直觀且功能完整的數獨體驗，並具備多種難度級別和輔助功能。

## 功能與設計規格

### 1. 介面佈局

*   **標題**: 頁面頂部顯示一個主標題 "數獨"。
*   **難度選擇**: 標題下方提供一個下拉式選單，用於選擇遊戲難度（簡單、中等、困難）。
*   **驗證結果**: 難度選擇下方，數獨棋盤上方，會顯示驗證結果的訊息。
*   **遊戲棋盤**: 頁面中央是 9x9 的數獨棋盤。
*   **數字面板**: 棋盤下方是**兩行五列**的按鈕佈局，包含數字 1-9 及一個 'X' 按鈕用於清除格子內容。
*   **操作按鈕**: 頁面底部包含三個主要操作按鈕：
    *   `驗證答案`: 檢查目前填寫的答案是否正確。
    *   `新遊戲`: 點擊後會根據當前選擇的難度，產生一個全新的數獨遊戲。
    *   `直接看答案`: 在棋盤上顯示正確的完整解答。

### 2. 視覺樣式 (CSS)

*   **棋盤格線**:
    *   使用清晰的格線來劃分棋盤。
    *   棋盤背景呈現**棋盤格樣式**，相鄰的格子有不同的背景顏色（例如，淺灰色和白色交錯）。
*   **數字樣式**:
    *   **題目數字**: 預設的題目數字使用深灰色文字。
    *   **玩家輸入數字**: 玩家填寫的數字使用 **粗體黑色** 文字。
    *   **衝突數字**: 當輸入的數字造成衝突時，所有衝突的數字應以 **紅色文字** 顯示。
*   **互動高亮效果**:
    *   **選取格子**: 當玩家點擊某個可填寫的格子時，該格子的背景色變為淺黃色。同時，該格子所在的整行和整列背景色也變為淺黃色。
    *   **選取數字**: 當玩家點擊數字面板上的某個數字按鈕時，棋盤上所有對應的數字文字顏色變為藍色，**並加上淺黃色底色**。
*   **按鈕狀態**:
    *   **數字完成**: 當棋盤上某個數字（例如 "3"）的 9 個格子都已被正確填寫後，數字面板上對應的 "3" 按鈕背景將變為淺灰色。

### 3. 功能邏輯 (JavaScript)

*   **遊戲初始化**: 頁面載入時，根據預設難度（簡單）生成一個新的數獨遊戲。
*   **新遊戲**: 點擊 "新遊戲" 按鈕後，系統會根據目前選擇的難度重新生成一個全新的數獨謎題，並重設所有狀態。
*   **數字填寫與衝突檢測**:
    *   玩家可以點擊棋盤上的空格，然後點擊數字面板上的數字來填寫。
    *   點擊 'X' 按鈕可以清除目前選定格子中的數字。
    *   每當玩家輸入或清除數字後，系統會自動檢查該數字所在的行、列及九宮格。如果出現重複數字（衝突），所有衝突的數字都會以紅色文字高亮顯示。如果沒有衝突，則恢復正常顏色。
*   **答案驗證**: 點擊 "驗證答案" 按鈕後，系統會檢查玩家填寫的數字是否與解答完全相符，並在上方顯示結果（成功 "恭喜！答案正確！🥳" 或失敗 "噢！還有一些錯誤，請再試一次。😢"）。
*   **查看答案**: 點擊 "直接看答案" 按鈕後，程式會自動將數獨的所有空格填上正確的答案。

### 4. 題目生成與難度

*   **題目可解性**: 所有生成的數獨題目都必須經過演算法驗證，確保其至少有一個解。
*   **答案唯一性**: 所有生成的數獨題目都必須保證只有一個唯一的解，以避免在驗證時出現因多解而導致的錯誤。
*   **難度調整**:
    *   **中等**: 降低「中等」難度的題目困難度，使其有別於「困難」等級。

### 5. 本地化

*   所有在介面上顯示的文字，包括按鈕、標籤和提示訊息，都使用 **繁體中文**。
